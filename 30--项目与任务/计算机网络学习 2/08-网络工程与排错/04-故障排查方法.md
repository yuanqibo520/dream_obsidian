# 网络故障排查方法

## 分层排查

1. 物理层：网线、无线信号、接口状态。
2. 数据链路层：MAC、VLAN、交换机端口。
3. 网络层：IP、网关、路由、ICMP。
4. 传输层：端口、防火墙、TCP 状态。
5. 应用层：协议、认证、服务日志。

## 常见现象

- 能 ping IP，不能访问域名：多半是 DNS 问题。
- 能 ping，端口不通：可能服务未监听或防火墙阻断。
- 间歇性断开：关注丢包、抖动、无线干扰、设备负载。

