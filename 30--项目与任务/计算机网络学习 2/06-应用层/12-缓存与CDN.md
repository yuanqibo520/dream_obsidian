# 缓存与 CDN

## HTTP 缓存

HTTP 缓存可以减少重复请求，提高访问速度并降低服务器压力。

## 强缓存

浏览器在缓存有效期内直接使用本地缓存。常见响应头包括 `Cache-Control` 和 `Expires`。

## 协商缓存

浏览器向服务器确认资源是否变化。未变化时服务器返回 `304 Not Modified`。

## CDN

CDN 把内容分发到靠近用户的边缘节点，减少 RTT，提高访问速度。

