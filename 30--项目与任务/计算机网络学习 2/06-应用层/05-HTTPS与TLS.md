# HTTPS 与 TLS

## HTTPS 是什么

HTTPS = HTTP + TLS。它提供加密、身份认证和完整性保护。

## TLS 提供什么

- 机密性。
- 完整性。
- 身份认证。

## TLS 握手核心

客户端和服务端协商协议版本、密码套件、随机数和会话密钥，并验证证书链。

## 证书验证

浏览器会验证证书是否由可信 CA 签发、域名是否匹配、是否过期或被吊销。

