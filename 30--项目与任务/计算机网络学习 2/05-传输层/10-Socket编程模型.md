# Socket 编程模型

## Socket 是什么

Socket 是应用程序使用网络能力的编程接口。

## TCP 服务端流程

1. 创建 socket。
2. 绑定 IP 和端口。
3. 监听连接。
4. 接受客户端连接。
5. 收发数据。
6. 关闭连接。

## TCP 客户端流程

1. 创建 socket。
2. 连接服务端。
3. 收发数据。
4. 关闭连接。

## UDP 流程

UDP 无连接，通常直接使用 `sendto` 和 `recvfrom` 发送、接收数据报。

## 学习重点

Socket 位于应用程序和传输层之间，是理解网络编程的重要桥梁。

