# 差错检测

## 为什么需要差错检测

数据在传输过程中可能受噪声、干扰和硬件问题影响而出错。差错检测用于发现帧是否被破坏。

## 奇偶校验

奇偶校验通过增加 1 位校验位，让 1 的个数满足奇数或偶数要求。

优点是简单，缺点是检测能力有限。

## 校验和

校验和把数据按固定长度分组求和，再用结果检测错误。IP、TCP、UDP 都涉及校验和思想。

## CRC

CRC 是循环冗余校验，以太网帧常用 CRC 检测差错。

特点：

- 检错能力强。
- 硬件实现高效。
- 只能检测错误，不能自动纠正错误。

