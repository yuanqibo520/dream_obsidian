# 停止等待与滑动窗口

## 停止等待协议

发送方每发送一个帧，就等待接收方确认，收到确认后再发送下一个帧。

优点是实现简单，缺点是链路利用率低，特别是在高 RTT 链路中。

## 滑动窗口协议

发送方可以连续发送多个帧，而不必每次都等待确认。

优点：

- 提高链路利用率。
- 支持流水线传输。

## 后退 N 帧

如果某个帧丢失，发送方从丢失帧开始重传后续多个帧。

## 选择重传

只重传丢失或出错的帧，效率更高，但实现更复杂。

