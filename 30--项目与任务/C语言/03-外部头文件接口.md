   ```C
#pragma once
#ifdef __cplusplus
extern "C"{
#endif
//定义节点数据类型
	struct Linknode
	{
		int data;
		struct Linknode * next;
	}
//初始化链表
	struct Linknode * init_LinkList();
//定位插入
	void insertByvalue _ LInkeList(struct Linknode * header,int oldval,newval);
//删除节点
	void RemoveByValue_ LinkList(struct Linknode* header,int DelValue)
//销毁
void Destroy_LinkList(struct Linknode* header)
//清空
void Clear_LinkeList(struct Linknode* header)

#ifdef __cplusplus
}
#endif 
daw 


```
