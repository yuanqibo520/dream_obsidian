# 封装与解封装

## 封装

发送数据时，每一层都会在上层数据外添加自己的头部信息。

典型过程：

1. 应用层产生数据。
2. TCP 添加 TCP 头部。
3. IP 添加 IP 头部。
4. 以太网添加帧头和帧尾。
5. 物理层转换为比特流。

## 解封装

接收数据时，每一层检查并移除对应头部，把载荷交给上层。

## 关键理解

同一份应用数据，在不同层有不同名字：报文、段、包、帧。

