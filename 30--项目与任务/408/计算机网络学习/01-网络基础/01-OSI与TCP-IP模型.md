# OSI 与 TCP/IP 模型

## OSI 七层模型

1. 物理层：传输比特。
2. 数据链路层：传输帧。
3. 网络层：跨网络转发 IP 包。
4. 传输层：端到端进程通信。
5. 会话层：会话管理。
6. 表示层：编码、压缩、加密等表示转换。
7. 应用层：面向用户应用的协议。

## TCP/IP 四层模型

1. 网络接口层。
2. 网际层。
3. 传输层。
4. 应用层。

## 为什么要分层

分层可以降低复杂度，让每层职责清晰，并便于协议替换和演进。

