# TCP 定时器

## 重传定时器

发送数据后，如果在超时时间内未收到 ACK，TCP 会重传数据。超时时间根据 RTT 动态估计。

## 坚持定时器

当接收窗口为 0 时，发送方不能继续发送数据。坚持定时器用于周期性探测窗口是否重新打开，避免双方永久等待。

## 保活定时器

保活定时器用于检测长时间空闲连接是否仍然有效。

## TIME_WAIT 定时器

主动关闭方在发送最后一个 ACK 后进入 TIME_WAIT，等待 2MSL，确保旧报文在网络中消失，并保证最后 ACK 可重传。

