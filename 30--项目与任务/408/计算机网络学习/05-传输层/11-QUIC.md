# QUIC

## QUIC 是什么

QUIC 是基于 UDP 的现代传输协议，HTTP/3 使用 QUIC 作为传输基础。

## 为什么基于 UDP

基于 UDP 可以在用户态实现新的传输机制，避免操作系统内核 TCP 升级缓慢的问题。

## 主要特点

- 内置 TLS 加密。
- 连接建立延迟低。
- 支持多路复用。
- 改善 TCP 队头阻塞问题。
- 支持连接迁移。

## 连接迁移

当用户从 Wi-Fi 切换到移动网络时，IP 地址可能变化。QUIC 可以通过连接 ID 维持连接连续性。

