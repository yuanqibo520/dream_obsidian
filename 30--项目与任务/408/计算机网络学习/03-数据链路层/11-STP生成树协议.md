# STP 生成树协议

## 为什么需要 STP

二层网络中如果存在环路，广播帧可能不断循环，引发广播风暴。

STP 用于在有冗余链路的交换网络中阻塞部分端口，形成无环逻辑拓扑。

## 核心概念

- 根桥：生成树的中心交换机。
- 根端口：到根桥路径最优的端口。
- 指定端口：某段链路上负责转发的端口。
- 阻塞端口：为避免环路而不转发数据帧的端口。

## 收敛

当拓扑变化时，STP 会重新计算端口状态。传统 STP 收敛较慢，RSTP 改进了收敛速度。

