# 进程创建与终止

## 进程创建

进程创建通常包括申请 PCB、分配地址空间、初始化资源、加入就绪队列等步骤。

## 进程终止

进程终止时，操作系统需要回收进程占用的内存、文件、设备、PCB 等资源，并通知相关父进程或等待者。

## 常见原因

- 正常结束。
- 出错退出。
- 被其他进程终止。
- 父进程结束导致子进程处理。

## 复习提示

Unix/Linux 中常见的 `fork` 创建子进程，`exec` 装入新程序，`wait` 等待子进程结束。

