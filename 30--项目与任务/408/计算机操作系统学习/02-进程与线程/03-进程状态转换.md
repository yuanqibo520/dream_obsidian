# 进程状态转换

## 基本状态

进程通常包含就绪、运行、阻塞三种基本状态。

## 状态含义

- 就绪：已获得除 CPU 外的所有必要资源，等待调度。
- 运行：正在 CPU 上执行。
- 阻塞：等待某个事件完成，例如 I/O 完成。

## 典型转换

- 就绪到运行：被调度程序选中。
- 运行到就绪：时间片用完或被抢占。
- 运行到阻塞：等待 I/O 或同步事件。
- 阻塞到就绪：等待的事件发生。

