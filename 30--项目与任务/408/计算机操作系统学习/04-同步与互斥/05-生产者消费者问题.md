# 生产者消费者问题

## 问题描述

生产者向缓冲区放入产品，消费者从缓冲区取出产品。缓冲区容量有限，需要同时解决互斥和同步。

## 需要的信号量

- `mutex`：保护缓冲区互斥访问，初值为 1。
- `empty`：表示空缓冲区数量，初值为缓冲区大小。
- `full`：表示满缓冲区数量，初值为 0。

## 核心顺序

生产者先等待 `empty`，再进入互斥区；消费者先等待 `full`，再进入互斥区。

## 易错点

不要把等待资源的 P 操作放在互斥锁之后，否则可能导致死锁。

