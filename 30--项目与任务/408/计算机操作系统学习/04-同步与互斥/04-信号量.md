# 信号量

## 核心概念

信号量是一种同步工具，通常通过 `P` 操作和 `V` 操作控制资源访问。

## 类型

- 二元信号量：取值通常为 0 或 1，可用于互斥。
- 计数信号量：表示某类资源的可用数量。

## 操作含义

- `P` 操作：申请资源，若无资源则阻塞。
- `V` 操作：释放资源，可能唤醒等待进程。

## 常见考点

信号量既可以用于互斥，也可以用于同步；关键是初值和 P/V 顺序。

