# 内核态与用户态

## 核心概念

现代处理器通常提供至少两种运行级别：用户态和内核态。普通应用程序运行在用户态，操作系统内核运行在内核态。

## 区别

- 用户态：权限受限，不能直接访问关键硬件和内核数据结构。
- 内核态：权限高，可以执行特权指令，管理硬件资源。

## 切换场景

用户态进入内核态通常发生在系统调用、中断、异常时。

## 常见考点

区分“状态切换”和“进程切换”。系统调用会导致用户态到内核态的切换，但不一定发生进程切换。

