# DMA

## 核心概念

DMA 是直接内存访问技术。它允许设备控制器在不经过 CPU 逐字节搬运的情况下，直接与内存交换数据。

## 工作过程

CPU 设置 DMA 控制器的内存地址、传输长度和方向。DMA 控制器完成数据传输后，通过中断通知 CPU。

## 优点

减少 CPU 参与，提高大块数据传输效率。

## 常见考点

DMA 并不是完全不需要 CPU，而是 CPU 只负责初始化和结束处理。

