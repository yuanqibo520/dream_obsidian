```C
void InsertSort(int A[],int n)
{
	int i = 0,j = 0;
	for(int i = 2;i<=n;i++)
	{
		if(A[i]<A[i-1])
			A[0]=A[i];
			int low = 1,high=i-1;
			while(low<high)
			{
				int mid = low+high) / 2;
				if(A[mid]>a[i])
					high = mid-1;
				else
					low = mid+1;
			}
			for(int j=i-1;j>=A[high-1];j--)
			{
				if(A[j+1]>A[0])
					A[high-1]=A[j];
			}
			A[j]=A[0];
	}
}
```