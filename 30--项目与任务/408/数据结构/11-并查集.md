```C
#include<stdio.h>

#define size 50

void Initial (int S[])
{
	for(int i =0;i<size;i++)
	S[i]=-1;
}//初始化函数

int Find(int S[],int x)
{
	int root =x;
	while(S[root]>0)
	root=S[root];
	while(S[x]!=root)
	{
		int t = S[x];
		S[x]=root;
		x=t;
	}
	return root;
}

void Union (int S[],int root1,int root2)
{
	if(root1==root2)
	return ;
	if(S[root1]>S[root2])
		S[root2]+=S[root1];
		S[root1]=root2;
	else
		S[root1]+=S[root2];
		S[root2]=root1;
	
}
int main()
{
	int size = 0;
	scanf("%d",&size);
	int UFSets[size]={0};
	Initial(UFSets);
	int root=Find(UFSets,x);
	return 0;
}
```