```C
void swap(int x,int y)
{
	int temp = x;
	x = y;
	y = temp;
}
void BubbleSort (int A[],int n)
{
	for(int i =n;i>0;i--)
	{
		bool flag = false;
		if(A[i]>A[i-1])
			swap(A[i],A[i-1])
			flag = true;
		if(flag == false)
		return ;
	}
}
```