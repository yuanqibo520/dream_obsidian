```C
#include<stdio.h>
#include <stdbool.h>

BiTNode pre = NULL;//辅助指针
typedef struct BiTNode{
	Elemtype data;
	struct BiTNode*Lchild,*Rchild;
	int ltag=0;
	int rtag=0;
}BiTNode ,*BiTree ;
void InitBiTNode(BiTree T)
{
	T->Lchild=NULL;
	T->Rchild-NULL;
}//初始化
void InThread(BiTree T)
{
	if(T->Lchild == NULL)
	{
		T->Lchild = pre;
		T->ltag = 1;
	}
	if(pre!=NULL&&pre->Rchild==NULL)
	{
		pre->Rchild = T;
		pre->rtag = 1;
	}
	pre = T;
}//中序线索化,一边遍历一边线索化

void CreateInThread (BiTree T)
{
	
	BiTNode p = q;//记录目标结点
	if(T!=NULL)
		InOrder(BiTree T)
	if(pre->rchild==NULL)
		pre->rtag=1;
}//创建线索二叉树

void InOrder(BiTree T)
{
	while(T!=NULL)
	{
		InOrder(T->Rchild);
		InThread(T);
		InOrder(T->Lchild);
	}
	while(T!=NULL)
	{
		InThread(T);
		if(T->ltag==0)
			InOrder(T->Rchild);
		InOrder(T->Lchild);
	}//前序
}//中序遍历

typedef struct LinkNode{
	BiTNode * data;
	struct LinkNode *next;
}LinkNode;
typedef struct LinkQueue{
	struct LinkNode *front,*next;
}LinkQueue;//定义辅助指针

bool InitLinkNode(LinkQueue &Q)
{
	Q.rear=Q.front=(LinkNode *)malloc(sizeof(LinkNode));
	Q.rear->next=NULL;
}//初始化辅助队列

bool EnLinkQueue(LinkNode &L,BiTree x)
{
	LinkNode *s=(LinkNode *)malloc(sizeof(LinkNode));
	s->data=x;
	s->next=NULL;
	Q.rear->next=s;
	Q.rear=s;
}
bool DeQueue(LinkQueue &Q, BiTNode &T)
{
	if(Q.rear==Q.front)//带头节点
		return false;
	LinkNode *s=Q.front->next;
	T=s->data;
	Q.front->next=s->next;
	if(Q.rear==s)
		Q.rear=Q.front;
	free(s);
	return 0;
}
bool IsEmpty(LinkQueue *Q)
{
	if(Q.rear==Q.front)
		return true;
	else
		return false;
}
void LevelOrder(BiTree T)
{
	LinkQueue Q;
	InitLinkNode(&Q);
	EnLinkQueue(Q,T);
	while(!IsEmpty(Q))
	{
		DeQueue(Q,T);
		Visit(T);
		if(T->Lchild!=NULL)
			EnLinkQueue(Q,T->Lchild);
		if(T->Rchild!=NULL)	
			EnLinkQueue(Q,T->Rchild);
	}
}
int main()
{
	BiTNode T;
	InitBiTNode(T);
	InOrder(T);
	LevelOrder(&T)
	return 0;
}
```