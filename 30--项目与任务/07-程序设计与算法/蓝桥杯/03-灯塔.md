```c
#include<stdio.h>
int main(){
	int n = 0 ,m = 0;
	scanf("%d %d",&n,&m);
	int count[n+1]={0};
	int temp = 0 ;
	for(int i = 0;i < m ; i++){
		scanf("%d",&temp);
		count[temp] = 1;
	}
	
	int res = 0;
	for(int i = 1 ;i<=n  ;）{
		if(count[i]==1)
		{
			i+=2;
			res++;
		}
		else
		{
			i++;
		}
	}
	printf("%d",res);
	return 0;
}
//死循环风险：你的 `for` 循环条件是 `i<=n`，但循环体内没有 `i++`，而是通过 `if-else` 改变 `i`。当 `i` 最终增加到 `n+1` 时，循环无法退出，导致程序卡死。
#include<stdio.h>
int main(){
	int n = 0 ,m = 0;
	scanf("%d %d",&n,&m);
	int count[n+1]={0};
	int temp = 0 ;
	for(int i = 0;i < m ; i++){
		scanf("%d",&temp);
		count[temp] = 1;
	}
	
	int res = 0;
	int i = 0;
	while(i<=n){
		if(count[i]==1)
		{
			i+=2;
			res++;
		}
		else
		{
			i++;
		}
	}
	printf("%d",res);
	return 0;
}
```

![[Pasted image 20260329180213.png]]