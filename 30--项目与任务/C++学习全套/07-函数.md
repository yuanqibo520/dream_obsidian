# 函数

## 函数定义

```cpp
int add(int a, int b) {
    return a + b;
}
```

## 函数声明

```cpp
int add(int a, int b);
```

声明告诉编译器函数存在，定义提供具体实现。

## 参数传递

```cpp
void by_value(int x);
void by_reference(int& x);
void by_const_reference(const std::string& s);
```

建议：

- 小对象可以按值传递。
- 大对象优先用 `const T&`。
- 需要修改实参时用 `T&`。

## 函数重载

```cpp
int max_value(int a, int b);
double max_value(double a, double b);
```

同名函数可以根据参数类型或数量区分。

## 默认参数

```cpp
void print(std::string text, int times = 1);
```

