# 指令类型

## 数据传送类

用于寄存器、内存、I/O 之间的数据移动。

例如：

- LOAD
- STORE
- MOV

## 算术逻辑类

用于加减乘除、逻辑运算、移位等。

例如：

- ADD
- SUB
- AND
- OR
- XOR
- SHL

## 程序控制类

用于改变程序执行顺序。

例如：

- JMP
- CALL
- RET
- 条件转移

## 输入输出类

用于 CPU 与外设交换数据。

## 特权指令

只能在内核态执行，例如修改中断屏蔽、访问特殊寄存器。

## 常考点

区分访存指令、运算指令和控制转移指令的执行过程。
