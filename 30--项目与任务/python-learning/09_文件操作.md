# 文件操作

## 读取文件

```python
with open("data.txt", "r", encoding="utf-8") as file:
    content = file.read()
    print(content)
```

## 按行读取

```python
with open("data.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())
```

## 写入文件

```python
with open("output.txt", "w", encoding="utf-8") as file:
    file.write("hello\n")
```

## 追加内容

```python
with open("output.txt", "a", encoding="utf-8") as file:
    file.write("new line\n")
```

## 路径处理

推荐使用 `pathlib`：

```python
from pathlib import Path

path = Path("data.txt")
print(path.exists())
print(path.read_text(encoding="utf-8"))
```

## 练习

1. 读取一个文本文件并统计行数。
2. 将用户输入保存到文件。
3. 批量列出某个文件夹中的所有 `.txt` 文件。
