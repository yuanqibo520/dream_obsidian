# Day 10：函数基础

## 学习目标

- 能定义函数。
- 能使用参数和返回值。

## 编码任务

写一个 `math_utils.py`：

1. `add(a, b)` 返回和。
2. `is_even(n)` 判断偶数。
3. `average(numbers)` 返回平均值。
4. 在主程序中调用这些函数。

## 加分任务

为每个函数添加类型注解。

## 自测问题

- `return` 和 `print` 有什么区别？
- 为什么函数能减少重复代码？

## 提交清单

- `math_utils.py`
- 每个函数至少 2 个调用示例
- 今日总结
