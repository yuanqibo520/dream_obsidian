# Day 28：Web API 入门

## 学习目标

- 能写一个最小 Web API。
- 理解 GET 和 POST。

## 编码任务

使用 Flask 或 FastAPI 写一个 `app.py`：

1. `GET /` 返回欢迎信息。
2. `GET /users` 返回用户列表。
3. `POST /users` 添加用户。

## 加分任务

为用户添加 ID，并支持按 ID 查询。

## 自测问题

- API 返回 JSON 有什么好处？
- GET 和 POST 区别是什么？

## 提交清单

- `app.py`
- 接口测试结果
- 今日总结
