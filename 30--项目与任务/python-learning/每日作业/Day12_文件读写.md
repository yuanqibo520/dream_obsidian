# Day 12：文件读写

## 学习目标

- 掌握文本文件读写。
- 掌握 `pathlib`。

## 编码任务

写一个 `notes.py`：

1. 输入学习笔记。
2. 保存到 `notes.txt`。
3. 支持查看所有笔记。
4. 每条笔记单独一行。

## 加分任务

每条笔记前加上当前日期时间。

## 自测问题

- `w` 和 `a` 模式有什么区别？
- 为什么推荐使用 `with open()`？

## 提交清单

- `notes.py`
- 生成的 `notes.txt`
- 今日总结
