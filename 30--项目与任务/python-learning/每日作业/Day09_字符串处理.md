# Day 09：字符串处理

## 学习目标

- 掌握字符串切片。
- 掌握常用字符串方法。

## 编码任务

写一个 `text_tool.py`：

1. 输入一段文本。
2. 输出文本长度。
3. 输出去掉首尾空格后的文本。
4. 统计某个关键词出现次数。
5. 判断文本是否为回文。

## 加分任务

忽略大小写和空格判断回文。

## 自测问题

- 字符串可以修改吗？
- `strip()`、`split()`、`replace()` 分别做什么？

## 提交清单

- `text_tool.py`
- 3 组测试
- 今日总结
