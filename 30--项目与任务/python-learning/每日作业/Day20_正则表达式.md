# Day 20：正则表达式

## 学习目标

- 能用正则提取文本。
- 能做简单格式校验。

## 编码任务

写一个 `regex_tool.py`：

1. 从文本中提取手机号。
2. 从文本中提取邮箱。
3. 把多个空格替换成一个空格。

## 加分任务

验证日期格式 `YYYY-MM-DD`。

## 自测问题

- `\d+` 表示什么？
- `search()` 和 `findall()` 有什么区别？

## 提交清单

- `regex_tool.py`
- 3 段测试文本
- 今日总结
