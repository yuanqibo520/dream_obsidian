# Day 18：类型注解

## 学习目标

- 能为变量和函数添加类型注解。
- 能阅读带类型注解的代码。

## 编码任务

重写一个学生成绩程序：

1. 使用 `list[dict[str, str | int]]` 或自定义类。
2. 所有函数加类型注解。
3. 至少包含添加、查询、平均分功能。

## 加分任务

使用 `dataclass` 表示学生。

## 自测问题

- 类型注解会不会在运行时强制检查？
- `str | None` 表示什么？

## 提交清单

- `typed_students.py`
- 运行输出
- 今日总结
