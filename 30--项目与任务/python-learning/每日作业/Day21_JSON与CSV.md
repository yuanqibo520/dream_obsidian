# Day 21：JSON 与 CSV

## 学习目标

- 掌握 JSON 读写。
- 掌握 CSV 基础读取。

## 编码任务

写一个 `data_store.py`：

1. 用 JSON 保存任务列表。
2. 支持添加任务。
3. 支持查看任务。
4. 读取一个 CSV 文件并统计行数。

## 加分任务

把 CSV 内容转换成 JSON。

## 自测问题

- `json.loads()` 和 `json.load()` 有什么区别？
- CSV 适合什么数据？

## 提交清单

- `data_store.py`
- 示例 JSON 文件
- 今日总结
