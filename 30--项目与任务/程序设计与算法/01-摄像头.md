```C
#include<stdio.h>


 typedef struct LNode{
	Elemtype data;
	struct LNode *next;
} LNode,*LinkList;

LNode *Insort(int num )
{
	if(!num)
		return ;
	LNode (*A) [num+1] ;
	for(int i = 0 ; i<= num+1 ; i++)
	{
		A[i]=NULL;
	}
	for(int i = 1 ;i<=num ;i++)
	{
		int temp = 0 ;
		scanf("%d",&temp);
		if(scanf("%d",&temp))
			continue;
		scanf("%d",&temp);
		LNode * a = ( LNode* ) malloc (sizeof(LNode));
		LNode * temp1 = a;
		for(int i = 0 ;i < temp; i++ )
		{
			int temp2 = 0;
			LNode * b = ( LNode* ) malloc (sizeof(LNode));
			b->next = NULL;
			scanf("%d",&temp1);
			b->data = temp2;
			a->next = b ;
			a = b;
		}
		A[i] =  temp1;
	}
	return A;
}

Recursion（）


int Search(int num , LNode (*A) [num+1])
{
	for(int i = 1;i <=num;i++)
	{
		LNode * c =A[1];
		if(c)
		{
			c = c->next;
			int flag = 1;
			while
			{
				A[c->data]
				
			}
		}
		
	}
	return 
}

int main()
{
	int num = 0 ;
	scanf("%d",&num);
	LNode* A = Insort(num);
	int end = Search(num,A);
	if(end)
	printf("%d\n",end);
	else
	printf("YES\n");
	return 0;
}
//数组指针函数函数类型怎么设计
//依次对照下一个形成闭环，两两对照
```
![[Pasted image 20260319200014.png]]