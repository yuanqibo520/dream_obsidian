# 单调队列

单调队列通常用双端队列实现，用于维护窗口内最大值或最小值。

## 滑动窗口最大值

维护一个单调递减队列，队首始终是当前窗口最大值。

## 基本操作

1. 移除队首过期下标。
2. 从队尾移除不可能成为答案的元素。
3. 加入当前元素下标。
4. 队首即为当前窗口答案。

## 复杂度

每个元素最多入队一次、出队一次，总时间 `O(n)`。

## 易错点

- 队列保存下标，而不是只保存值。
- 过期判断通常是 `index <= right - k`。
- 队尾弹出条件与最大值或最小值目标有关。

