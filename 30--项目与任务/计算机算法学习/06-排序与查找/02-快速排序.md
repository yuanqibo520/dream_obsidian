# 快速排序

快速排序使用分治思想。

## 核心流程

1. 选择一个基准值。
2. 将小于基准的元素放左边，大于基准的元素放右边。
3. 递归排序左右两部分。

## 复杂度

- 平均时间：`O(n log n)`
- 最坏时间：`O(n^2)`
- 平均空间：`O(log n)`

## 为什么会退化

如果每次基准都选到最小值或最大值，递归树会退化成链，时间变成 `O(n^2)`。

## 优化方式

- 随机选择基准。
- 三数取中。
- 对大量重复元素使用三路快排。

## 易错点

- 分区时指针移动条件写错。
- 基准值位置处理不清。
- 重复元素导致死循环。

