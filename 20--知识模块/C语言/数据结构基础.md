```C
typedef struct {
    int * base;      // 栈底指针
    int * top;       // 栈顶指针（指向下一个可插入位置）
    int stacksize;  // 当前分配的容量（元素个数）
} Sqstack;

void Init (Sqstack* s)
{
	s.base=(int * )malloc(size* sizeof (int));
	s.top=s.base;
	s.stacksize=size;
}

void push (Sqstack* s,int e )
{
	if(s.top-s.base>s.stacksize)
	{
		// 栈满，扩容：增加 increasize 个 int 的空间
		s.base=(int * )realloc(s.base,(s.stacksize+increasize)* sizeof(int));
		// 重新设置栈顶指针（因为 realloc 可能移动内存
		s.top =s.base+s.stacksize;
		// 更新栈容量
		s.stacksize+=increasize;
	}
	// 将元素 e 放入栈顶，并移动栈顶指针
	* s.top++ =e;
}
 void pop(Sqstack* s,int e)
 {
	 
 }
int pop(Sqstack * s, int * e)
{
    if (s->top == s->base) 
    {
        return 0; 
    }
    // 栈顶指针先回退，再取值（因为 top 指向下一个空位置）
    s->top--;
    **(* e = * (s->top); )   )**
    return 1;  // 成功弹出
}
void print (Sqstack* s)
{
	int * temp;
	temp=s.top;
	while(temp!=s.base)
	{
		temp--;
		printf;
	}
}

```

