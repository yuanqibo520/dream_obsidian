---
tags:
  - 计算机语言
created: 2025-12-17
status: in_progress
---
---
## 核心要点
- 


---
## 应用场景
- 

---
## 关联概念
- [[ ]]

---
1.变量和局部变量，以局部变量优先.
2.文件的第一行加上#define _CRT_SECURE_NO_WARNINGS 0
3.作用域的运用法则同括号相似
4.extern int申明外部符号 
5.const+关键字 修饰的常变量 不许被改变
6.enum color{
red
yellow
blue
}枚举常量
7.char arr[]="abcdef" 字符串的末尾隐藏着\0
a=strlen(arr1)-1=sizeof(arr1)/sizeof(arr1[0])-2
8.printf("%d\n",strlen=()); 需要调用库 #include《string.h》用于计算字符串的字数
8.%.（数字）f加入可精确小数点后位

25.9.20
tab是水平制表 即跳动水平一定位置
\n 换行 \0结束 \ddd八进制三位数 \xhh16进制两位数字 \\ \? \'
a=strlen
sqrt开平方
a65 A92

25.9.22
学习画图 理解内存 指针
&为取地址符号每次记得打 包括数组

25.9.24
！逻辑反操作符
sizeof （int） 计算操作数的类型长度 单位字节
sizeof（arr【】）计算数组内元素的大小 
sizeof（arr）计算数组的字符
元素个数=sizeof（arr)/sizeof（arr【】）
exp1?exp2：exp3;  通过判断exp1的真假，得出exp2还是exp3的值
逗号表达式的计算是自左向右，整个表达式的结果是最后一个值

25.10.2
#include《iostream>
using namespace
cout<<"hello world<<endl;
次方pow(num,1.0/n.0)

25.10.9
typedef 重新定义 在int main外使用
static
修饰全局变量  外部连接属性变成内部连接属性
修饰局部变量  出了作用域 变量不被销毁
修饰函数 函数同样具有外部链接属性
extern 链接外部变量和函数 及不同文件
register 建议放在寄存器 决定权在编译器

25.10.11
int*p *p *p为字符地址变量取（指针变量） 
*说明p是指针变量
不管是什么类型都是指针变量 

25.10.13
EOF==-1
int sum=getchar();	
putchar(sum);
在循环语句中表示输入内容与指定字符不相等时使用‘0’来表示
数组int arr[20]={0} 花括号里面的0是进行了初定义
%02d/n，表示打印两个整数 用0来填充第一个位置
float a=0.0f f可以使0.0表示为值 而不是double类型 0.0默认是double类型
%.2f在小数位数大于2的类型中输出时可以自动四舍五入
换行为\n
打印字符串时printf("printf(\"hello,world\\n\")\n"
将“和\n进行转译字符而不是操作符
输出数字精度不够的话换double %lf为	

25.10.16
Sleep(10000)单位毫秒 头文件为#include《window.h>
system("cls") 清空屏幕 头文件 #include《stdlib.h》
比较两个字符串是否相等 strcmp（arr，“dakwkdjawd”==0）头文件include《string.h》

25.10.29
if(可以直接判断值是否为1/0)1执行 0不执行
return 是直接结束函数

25.11.1
switch语句中default可以代表其他输入数字
调用rand前使用srand（（int unsigned）time（NULL））
time用时间戳生成随机值 头文件time.h
crand rand 头文件为stdlib.h
strcmp比较数组大小 相等为0，strcmp（a，b) 小于输出负数 大于输出正数
system（“shutdown -s -t 60”）；
t为设置 s为关机 a为取消关机
again：
printf（“hello，world”）；
goto again；

2025.11.13
數組大小由變量定義時不能不初始化
如果是常數可以初始化
函數傳輸數組是地址符 不能再函數内用sizeof計算數字的元數個數
&數組名是整個數組的地址
二維數組取地址是第一行的數組地址

2025.11.18
a=3
b=-5
c=a 符号 b
&补码两者有0为0
|补码两者有1为1
^补码相异为1，相同为0
a^a=0
0^a=a
if的表达：0为假，非0为真

2025.11.20
32位机器 指针变量为4个字节，64位机器 指针变量为8个字节
%u是无符号整数
指针类型被解引用的时候访问不同字节

2025.11.24
野指针成因未初始化，越界访问
设置结构体为一个档案带，得预设其中的所有内容，在主函数中定义结构体变量或者数组，也可以在结构体尾巴处定义变量
结构体指针的定义要带上名字，指针->成员来表示，
初定义可以指定顺序{.成员=XX，.成员=XX}
计算结构内存大小考虑对齐规则
首个成员首项对齐，每个成员首地址必须在对应变量字节的倍数上，总内存大小为结构体内最大变量字节的整数倍，其他空开的内存编译器自动填充
字符串不可以直接用=赋值，
得用strcpy（XX.XX，”XX“）；头文件为include<string.h>
结构体嵌套为在父结构体内部定义一个结构体变量， 在其外部定义一个子结构体档案袋
strcmp比较数组大小 相等为0，strcmp（a，b) 小于输出负数 大于输出正数,用于判断结构体内字符串是否对应


天和核心艙（Tianhe）發射	2021-04-29 11:23:15
神舟十二號（Shenzhou-12）發射	2021-06-17 01:22:27 UTC（即北京時間約 09:22） 
夢天實驗艙（Mengtian / 夢天）發射	2022-10-31 15:37:23.191（北京時間） 
夢天實驗艙與火箭分離入軌	約 08 分鐘後發射（根據官方報導） 
夢天實驗艙太陽能翼展開	約 2022-10-31 16:30（北京時間） 
天宮一號 發射（2011 年）	2011-09-29 21:16:03（北京時間）
入軌時間：21:25:45 
神舟九號 發射（2012 年）	2012-06-16 18:37:24（北京時間） 
嫦娥三號 發射（2013 年）	2013-12-02 01:30:00.344（北京時間） 
嫦娥五號 發射（2020 年）	2020-11-24 04:30:00（北京時間）