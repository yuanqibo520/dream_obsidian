```C
#include<stido.h>
#include<string.h>
#define max_size 10
typedef struct 
{
	char data[max_size];
	int top;
}SqStack;

InitSqQueue(SqQueue &S)
{
	top=-1;
}

bool Push(SqStack &S,char x)
{
	if(S.top==(MaxSize-1))
	return false;
	S.data[++S.top]=x;
	return ture;
}//进栈操作

bool Pop(SqStack &S,char &x)
{
	if(S.top==-1)
	return false;
	x=S.data[S.top--];
	return ture;
}//出栈操作

bool Emptystack(SqStack &S)
{
	if(S.top==(MaxSize-1))
	return ture;
}


bool bracketcheck (char arr[],int length)
{
	SqQueue S;
	InitSqQueue(S);
	for(int i = 0; i<length ; i++)
	{
		if(arr[i]=='('||arr[i]=='{'||arr[i]=='[')
			Push(S,arr[i])
		else if
		{
			if(Emptystack(S))
				return false;
			else
			{
				char Elemtop=0;
				Pop(S,Elemtop);
				if(arr[i]==')'&& Elemtop!='(')
					return false;
				if(arr[i]==']'&& Elemtop!='[')
					return false;
				if(arr[i]=='}'&& Elemtop!='{')
					return false;
			}
		}
	}
	return Emptystack(S);
}
int main()
{
	char arr[]={0};
	scanf("%s",arr);
	int length=strlen(arr);
	bracketcheck(arr,length);
	return 0;
}
```
