``` C
#include<stdio.h>
#include<stdlib.h>

#define Init_size 10

typedef struct {
	int * data;
	int length;
	int Max_size;
}SeqList;//Sequence_list 

void InitList(SqList &L) {
	L.length = 0;
	L.Maxsize = Init_size;
	L.data = (Elemtype*)malloc(sizeof(Elemtype) * Init_size);
}

IncreaseSize(SqList& L, int len) {
	Elemytype* p = L.data;
	L.data = (ElemType*)malloc(sizeof(Elemtype) * (L.Maxsize + len));
	for (int i = 0; i < L.length; i++) {
		L.data[i] = p.data[i];
	}
	L.Maxsize = p.Maxsize + len;
	free(p);
}

bool ListInsert(SqList &L, int i, int e) {
	if (i<1 || i>L.length)
		return false;
	if (L.length >=L.Max_size)
		return false;
	for (int j = L.length; j >= i; j--)
		L.data[j] = L.data[j - 1];
	L.data[i] = e;
	L.length++;
}
bool ListDelete(SqList &L, int i, int &e) {
	if (i<1 || i>L.length)
		return false;
	e = L.data[i];
	for (int j = i; i < L.length; j++)
		L.data[j] = L.data[j + 1];
	L.length--;
	return ture;
}

int GetElem(SqList &L,int i){
return L.data[i];
}

LocateElem(SqList &L,int e){
	for(int i = 0;i<L.length;i++)
	{
		if(L.data[i]==e)
		return i+1;
	}
	return 0;
}

int main()
{
	int i = 0, e = 0;
	SqList L;//创建顺序表
	InitList(L);//初始化
	IncreaseSize(L, e);//增大内存空间,e为添加量
	if (ListInsert(L, i, e))//i位置插入e元素
		printf("成功插入\n");
	else
		printf("输入位置有误\n");
	int e = -1;//删除i位置元素，用e返回被删除值
	if (ListDelete(L, i, &e))
			printf("成功删除:%d\n", e);
	else
			printf("输入位置有误\n");
	int num=GetElem(L,i);
	printf("查找到了，是%d"，num);
	int lot=LocateElem(L,e);
	printf("找到了，在%d位\n",lot);
	//...
	free(L.data); // 释放内存
	return 0;
}

```