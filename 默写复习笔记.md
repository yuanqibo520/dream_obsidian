---
data: 2026-01-28
---
```bash
tab 自动补全
ls -a 显示隐藏文件
ls -l 显示详细内容
ls -lh 显示文件大小
less <name> 查看长文件 上下换行 q退出
more <name> 查看长文件 space换页 q退出
mv <oname><nname> 修改文件名
mv <name> <path> 修改文件路径
cat > <name><body>^D 创建文件并输入内容（按 Ctrl+D 结束输入）
cat <name> 查看文件内容
cp <name><path> 复制文件
cp -r <name><path> 复制路径
rm <name> 删除文件
rm -r <name> 递归地复制文件
rm -rf <name> 强制地递归地复制文件
mkdir <name> 创建文件夹
mkdir -p <dir1><dir2> 递归地创建文件夹
touch <name> 创建文件
pwd 查看当前文件路径
2026.1.28
```
---
```bash
cd - 
mkdir -p <path1>/<path2>
cat > <file.name><body>^d
.<-.path>
tar -zxvf <name.tar.gz> <goal.path>
tar -xcvf <name.tar.gz> <file1> <file2>
sudo apt upgrade
sudo apt update
sudo useradd -m <username>
chmod (u/g/a/o/' ')o (-/+) (w/r/x) <name.py>
2026.1.30
```

